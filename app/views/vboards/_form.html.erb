<%= form_with model: @vboard, local: true do |form| %>
  <% if  @vboard.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(vboard.errors.count, "error") %>
          prohibited this VBoard from being saved:
      </h2>
      <ul>
        <% @Vboard.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </p>
  <p>
    <%= form.label :video %>
    <%= form.file_field :video %>
  </p>
  <p>
    <%= form.submit %>
  </p>

<% end %>
